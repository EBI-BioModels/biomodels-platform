# Shared dependency versions for jummp-biomodels and its inline plugins.
#
# Declared here once and read from grails-app/conf/BuildConfig.groovy and the
# jummp-plugins/*/grails-app/conf/BuildConfig.groovy files, so a coordinated
# version bump (e.g. AnnotationStore) is a single edit rather than three.

# net.biomodels.jummp:annotationstore - externalised core domain model
# (sibling repo ../annotationstore, published to GitHub Packages). Cut the release there, then
# bump here. ../biomodels-indexer/pom.xml pins its own copy of this version - bump it too.
annotationStore.version=0.3.7-SNAPSHOT
